# 里程碑 B 技术决策

> 文档编号：ADR-B-001
> 状态：已确认，可用于准备工作
> 适用分支：`feature/continuous-omni`
> 决策日期：2026-07-30
> 目标：将声音变成 JoyAI-VL 的原生输入，并形成可持续增量理解的 Streaming Thinker

## 1. 文档目的

本文记录进入里程碑 B 前已经确认的产品与技术边界，避免后续因训练框架、数据来源或短期实现便利而偏离目标。

里程碑 B 的重点是原生音频理解和增量思考，不包含端到端语音生成。所有正式训练必须在模型结构、数据许可、训练资源和验收指标完成检查后开始。

## 2. 已确认的核心决策

### 2.1 里程碑 B 的完成范围

里程碑 B 完成以下能力：

- JoyAI-VL 原生接收音频 embedding，不只依赖 ASR 文本；
- 音频、视觉和文本使用一致的时间信息组织；
- Thinker 可以在连续输入期间增量更新理解和决策；
- Thinker 输出增量文本、动作和语义状态；
- 支持 `silence`、`response`、`delegate`、`interrupt` 动作；
- 保持纯文本、纯图像和图文输入兼容；
- 保留里程碑 A 已有的时间线、回声过滤、优先级、打断和生命周期控制；
- 使用独立 TTS 完成语音输出。

里程碑 B 不包含：

- 自回归语音 codec token 生成；
- Thinker hidden states 直接驱动 Talker；
- Thinker–Talker 端到端联合训练；
- 音色克隆；
- Talker 的音色、韵律和说话人一致性训练。

以上内容放入里程碑 C。除非重新评审本文，不在 B 阶段提前扩大范围。

### 2.2 主体模型

继续以 `jdopensource/JoyAI-VL-Interaction` 8B 为主体模型。

Qwen、MiniCPM 等模型可以作为以下角色：

- 音频 Encoder 来源；
- 教师模型；
- 自动或半自动数据标注模型；
- 对照模型。

它们不替代 JoyAI-VL 成为系统的主体模型。

### 2.3 Thinker 与 Talker 的连接方式

第一版采用增量语义桥接，不采用纯文本最终结果接口，也不直接耦合 Thinker hidden states。

建议的逻辑输出包含：

```text
ThinkerDelta
  session_id
  generation_id
  time_range
  action
  text_delta
  semantic_tags
  prosody_hint
  confidence
  committed
```

具体字段可在实现阶段调整，但必须保留以下语义：

- 增量输出，而不是等待整段回答结束；
- 区分临时预测与已经提交的内容；
- 能关联输入时间区间；
- 能被 `generation_id` 取消；
- 动作和文本内容分离；
- Talker 不需要读取 JoyAI-VL 的内部隐藏维度。

如果后续实验确认语义桥接成为延迟、韵律或表达能力瓶颈，再单独评审 hidden-state bridge。

### 2.4 音频 Encoder 路线

第一版优先采用 Qwen 系列音频 Encoder。

在确定具体 checkpoint 和模型类之前，先检查：

- 是否能从完整模型中独立抽取 Encoder；
- 输入采样率、特征提取器和归一化方式；
- 输出 hidden size；
- 时间下采样比例；
- 是否支持分块和缓存；
- padding、attention mask 和 batch 规则；
- 权重及代码许可证；
- 与当前 PyTorch、Transformers 和训练框架的兼容性。

当前已经部署验证过 Qwen3-ASR，因此优先调查其音频 Encoder，但本文不提前锁定具体版本。

第一版不把 CLAP 直接并入主模型。先验证 Qwen Encoder 对以下非语音声音的理解：

- 火警和烟雾报警器；
- 爆炸；
- 玻璃破碎；
- 哭声、笑声等人类非文字声音；
- 音乐和普通背景噪声；
- 语音与环境声音重叠。

如果 Qwen Encoder 在实际需求中对环境声音语义明显不足，再评估：

- 增加 CLAP 等辅助 Encoder；
- 使用 CLAP 生成辅助监督；
- 保留 CLAP 作为高优先级旁路；
- 扩充环境声音指令数据。

是否加入双 Encoder 必须以实验结果为依据。

### 2.5 分阶段训练

不从全参数联合训练开始。

#### 阶段 1：音频—语言对齐

- 冻结 JoyAI 视觉 Encoder；
- 冻结 JoyAI LLM；
- 冻结 Qwen 音频 Encoder；
- 只训练 audio projector/resampler、模态标识和必要的时间表示；
- 首先验证语音内容、音频描述和基础声音事件。

#### 阶段 2：有限音频侧适配

- 继续训练 projector/resampler；
- 根据阶段 1 结果，允许解冻少量音频侧层；
- 不默认解冻完整音频 Encoder；
- 验证声音事件和连续语音表征是否提升。

#### 阶段 3：多模态指令微调

- 对 JoyAI 使用 LoRA，或在有证据时解冻少量层；
- 加入图像、视频、音频和文本联合任务；
- 保留一定比例的纯文本、纯图像和视频样本，减少视觉语言遗忘；
- 加入音视频一致、互补、冲突和缺失模态样本。

#### 阶段 4：流式动作训练

- 使用时间对齐的连续音视频窗口；
- 训练 `silence`、`response`、`delegate`、`interrupt`；
- 大量加入无事件、稳定背景和不应发言的负样本；
- 加入用户改口、重叠说话、系统说话期间用户插话和危险事件抢占。

#### 阶段 5：偏好优化

偏好优化为可选阶段，只有在监督微调后仍存在明显的误报、漏报、重复发言或打断时机问题时才启用。

全参数训练不属于默认路线。如果 projector、有限解冻和 LoRA 无法达到目标，需要重新评审训练资源和视觉能力遗忘风险。

## 3. 数据与教师模型决策

### 3.1 已允许

- 下载和使用外部开源音频、视频和多模态数据；
- 在研究实验中使用许可明确的非商用数据；
- 使用外部模型生成或辅助生成标注；
- 使用本地 Qwen-Omni、MiniCPM 等作为教师模型；
- 训练中文和英文；
- 包含情绪、语音与环境声重叠、多说话人重叠；
- 包含语音、非语言人声、环境声音、静音和音视频冲突；
- 允许跨夜运行训练。

暂不需要：

- 方言专项能力；
- 音色克隆；
- 说话人模仿。

### 3.2 许可分层

当前尚未决定最终模型是否必须满足商业使用和重新分发要求，因此数据必须分为至少两类：

```text
redistributable
  许可允许当前预期用途、训练和必要的结果发布

research_only
  仅用于研究或内部实验，不得据此宣称 checkpoint 可商用
```

每条训练数据必须记录：

- 数据集和原始来源；
- 数据版本；
- 许可证；
- 是否允许修改；
- 是否允许再分发；
- 是否允许商业使用；
- 是否允许训练模型；
- 是否为教师模型生成标注；
- 教师模型及生成配置；
- 被哪些训练运行使用。

不同许可数据不能在无记录的情况下混合。每个 checkpoint 必须能够追溯使用过的数据清单。

### 3.3 数据是否可以离开本地环境

该项尚未最终决定。采用以下临时默认：

> 原始音频、视频和内部标注不得离开本地服务器或本地工作环境。

因此准备阶段：

- 教师模型默认本地运行；
- 不向远程模型 API 上传媒体或内部标注；
- 不使用不明确的数据上传服务；
- 如果以后确需远程 API，必须重新获得用户授权；
- 远程调用时仍需检查数据许可证和隐私要求。

## 4. 训练资源决策

### 4.1 首期资源

第一版按两张 RTX 4090、每张 24GB 显存设计。

允许：

- 多卡训练；
- 跨夜运行；
- 任意合理的连续占用时长；
- 断点续训；
- 根据服务器磁盘容量决定 checkpoint 保留数量。

不得：

- 使用未明确授权的 GPU；
- 停止或迁移其他用户进程；
- 假设两张 24GB 显卡自动等同于一张 48GB 显卡；
- 在显存未实测前直接启动长训练。

### 4.2 资源可行性判断

两张 RTX 4090 预期足以完成：

- projector/resampler 训练；
- 冻结 Encoder 的音频—语言对齐；
- JoyAI 8B LoRA；
- 小 batch、有限上下文的音视频联合训练；
- 短训练和断点恢复验证。

两张 RTX 4090 不作为以下任务的默认资源：

- JoyAI 8B 全参数 BF16 训练；
- JoyAI 和音频 Encoder 全参数联合训练；
- 超长音视频上下文的大 batch 训练；
- Thinker–Talker 端到端联合训练。

首版训练配置原则：

- BF16；
- micro batch size 从 1 开始；
- gradient accumulation；
- gradient checkpointing；
- 冻结视觉 Encoder；
- 冻结大部分 JoyAI；
- 冻结 Qwen 音频 Encoder；
- projector 使用普通参数训练；
- JoyAI 适配使用 LoRA；
- 必要时评估 QLoRA；
- 音频 chunk 和上下文长度从较小配置开始；
- checkpoint 包含 optimizer、scheduler、scaler、随机状态和数据游标；
- 第一次只运行几十步显存与恢复测试。

两张 4090 是否满足完整阶段 3、4，必须由真实峰值显存、吞吐量和稳定性测试决定。如果明显不足，再申请更多 GPU。

### 4.3 Checkpoint 临时策略

在完成服务器磁盘审计之前，不固定保存数量。准备阶段先要求：

- 支持从最近 checkpoint 恢复；
- 定期保存训练状态；
- 至少保留最近 checkpoint 和一个已验证可加载的稳定 checkpoint；
- 保存前检查剩余磁盘；
- 不覆盖唯一可恢复 checkpoint；
- 保存数据版本、配置和代码 commit；
- 大模型权重不提交普通 Git。

## 5. 验收决策

最终验收指标尚未确定，可以在 B0 准备期间讨论，但必须在正式训练前冻结。

至少需要确定：

- 原生视觉语言能力允许下降多少；
- 单图、短视频、长视频和时间理解是否分别设最低线；
- 语音内容理解准确率；
- 非语音声音理解范围；
- 音视频一致、互补和冲突任务指标；
- 静音误触发率；
- `silence/response/delegate/interrupt` 动作指标；
- Streaming Thinker 更新延迟；
- 实时系数；
- 峰值显存和最长稳定运行时间；
- 是否要求中文、英文分别验收。

训练前后视觉语言能力的具体评估操作另见：

- `原生VLM基线评估操作说明.md`

该评估方案仍可继续讨论，本文不提前锁定其题目来源和最终基准组合。

## 6. 尚未锁定的工程参数

以下参数由调查和小规模实验决定，不属于当前产品决策：

- Qwen 音频 Encoder 的具体 checkpoint；
- 音频采样率；
- 音频 chunk 长度和重叠；
- 音频特征的时间下采样率；
- 每秒进入 JoyAI 的音频 token 数；
- projector/resampler 结构；
- 音频 embedding 与 JoyAI hidden size 的对齐方式；
- 音频、视觉、文本的位置编码；
- LoRA rank、target modules 和 dropout；
- 使用 LLaMA-Factory、自定义 Transformers trainer 或其他训练框架；
- 使用 FSDP、DeepSpeed ZeRO 或其他多卡策略；
- 训练 batch、梯度累积和学习率；
- checkpoint 频率和最终保留数量。

这些参数应通过可重复实验确定，并记录选择依据。

## 7. 进入正式训练前的关卡

必须全部满足：

- [ ] 完成原生 VLM 训练前基线；
- [ ] 确认 JoyAI 实际模型类、配置、chat template 和位置编码；
- [ ] 确认训练代码或确定自定义训练实现；
- [ ] 确认 JoyAI 权重、代码和数据许可证；
- [ ] 确认 Qwen 音频 Encoder 的提取方法和许可证；
- [x] 冻结第一版数据 schema；
- [ ] 建立数据许可和来源 manifest；
- [x] 完成假 Encoder 的 shape、mask、position 和 batch 测试；
- [ ] 完成 projector-only 单卡/双卡短训练；
- [ ] 验证 checkpoint 保存和恢复；
- [ ] 完成 2×4090 峰值显存与吞吐量记录；
- [ ] 冻结正式验收指标；
- [ ] 获得训练使用 GPU 的明确授权。

任何一项未完成，都不能把短训练扩展成长时间正式训练。

## 8. 下一步

下一项工作是 B1 可行性调查，顺序如下：

1. 盘点 JoyAI 的实际模型结构和训练入口；
2. 盘点 Qwen 音频 Encoder 的输入、输出、缓存和许可证；
3. 形成 JoyAI 与 Qwen Encoder 的张量兼容矩阵；
4. 设计最小 `AudioEncoder` 和 `AudioProjector` 接口；
5. 用假 Encoder 完成无 GPU 单元测试；
6. 根据模型参数和激活规模编制 2×4090 显存预算；
7. 输出是否可以进入 projector-only 短训练的 Go/No-Go 结论。

这一阶段不需要启动真实模型，也不需要占用 GPU。

## 9. 决策变更规则

以下变更必须由用户重新确认：

- 将主体模型从 JoyAI-VL 更换为其他模型；
- 把 Talker 或语音 codec 训练加入里程碑 B；
- 从语义桥接改为 hidden-state 直接耦合；
- 启用全参数训练；
- 使用远程 API 上传媒体或内部标注；
- 宣称使用 `research_only` 数据训练的 checkpoint 可商用；
- 增加音色克隆或说话人模仿；
- 在验收指标冻结后修改通过标准。

普通实现细节、测试组织和不改变上述边界的参数调优，不需要重复进行产品决策。
